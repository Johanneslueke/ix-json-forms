import { jsonFormsRenderer } from './json-forms-renderer';

describe('jsonFormsRenderer', () => {
  it('should work', () => {
    expect(jsonFormsRenderer()).toEqual('json-forms-renderer');
  });
});
